services:
  api:
    build:
      context: ../GithubDashboard.API
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
  ui:
    build:
      context: ../GithubDashboard.UI
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
  redis:
    image: redis
    ports:
      - "6379:6379"
